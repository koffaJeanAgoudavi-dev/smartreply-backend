name = "smartreply-backend"
main = "app/main.py"
compatibility_date = "2026-09-14"
compatibility_flags = ["python_workers"]

[vars]
ENVIRONMENT = "production"

# Forcer l'inclusion des dépendances Python
[build]
command = "pip install -t .python_packages -r requirements.txt"
